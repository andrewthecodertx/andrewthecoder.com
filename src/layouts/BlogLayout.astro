---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

export interface Props {
  title: string;
  description: string;
  permalink: string;
  current?: string;
  image?: string;
}
const { title, description, permalink, current, image } = Astro.props;
---
<html lang="en">
<head>
  <BaseHead title={title} description={description} permalink={permalink} image={image} />
</head>
<body class="bg-amber-50 text-black dark:bg-gray-800 dark:text-amber-50">
  <div class="flex flex-col min-h-screen">
    <Header current={current} />

    <main class="flex-1 py-8">
      <slot />
    </main>

    <Footer />
  </div>
</body>
</html>
