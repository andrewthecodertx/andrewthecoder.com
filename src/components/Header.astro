---
import Logo from './Logo.astro'
import Nav from './Nav.astro'

const { current = '' } = Astro.props;
---

<header class="flex justify-between items-center p-3 border-b-2 xs:flex-col xs:p-4">
  <Logo />
  <Nav current={current} />
</header>
